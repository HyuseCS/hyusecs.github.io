---
const links = [
  { name: "About", href: "#about" },
  { name: "Skills", href: "#skills" },
  { name: "Projects", href: "#projects" },
  { name: "Activity", href: "#activity" },
  { name: "Contact", href: "#contact" },
];
---

<nav class="sticky top-0 z-50 bg-surface-high/95 backdrop-blur-sm border-b border-system-border" role="navigation" aria-label="Main navigation">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-14">
      <div class="flex-shrink-0 flex items-center gap-2 pr-4 sm:pr-0">
        <span class="text-binary-green font-bold" aria-hidden="true">❯</span>
        <a href="#" class="text-xs sm:text-sm font-bold text-text-muted hover:text-binary-green transition-colors animate-glitch-hover whitespace-nowrap">CJ_Portfolio</a>
      </div>

      <!-- Desktop Nav -->
      <div class="hidden md:flex items-baseline space-x-4 sm:space-x-6">
        {links.map((link) => (
          <a
            href={link.href}
            class="text-text-dim hover:text-binary-green py-2 text-xs sm:text-sm font-medium transition-colors duration-200 animate-glitch-hover whitespace-nowrap"
          >
            <span class="text-text-deemphasized" aria-hidden="true">./</span>{link.name.toLowerCase()}
          </a>
        ))}
      </div>

      <!-- Mobile Hamburger -->
      <button
        id="hamburger-btn"
        class="md:hidden flex flex-col justify-center items-center w-8 h-8 gap-1.5 cursor-pointer group"
        aria-label="Open navigation menu"
        aria-expanded="false"
        aria-controls="mobile-drawer"
      >
        <span class="hamburger-line w-5 h-[2px] bg-text-dim group-hover:bg-binary-green transition-all duration-200 origin-center"></span>
        <span class="hamburger-line w-5 h-[2px] bg-text-dim group-hover:bg-binary-green transition-all duration-200 origin-center"></span>
        <span class="hamburger-line w-3.5 h-[2px] bg-text-dim group-hover:bg-binary-green transition-all duration-200 origin-center self-end"></span>
      </button>
    </div>
  </div>
</nav>

<!-- Mobile Drawer Overlay -->
<div id="drawer-backdrop" class="fixed inset-0 bg-black/60 z-[998] hidden" aria-hidden="true"></div>

<!-- Mobile Drawer -->
<div
  id="mobile-drawer"
  class="fixed top-0 right-0 h-full w-64 bg-layer-2 border-l border-system-border z-[999] hidden flex-col"
  role="dialog"
  aria-modal="true"
  aria-label="Navigation menu"
>
  <div class="flex items-center justify-between px-5 h-14 border-b border-system-border">
    <span class="text-text-dim text-xs font-mono uppercase tracking-widest">Navigation</span>
    <button id="drawer-close" class="text-text-dim hover:text-binary-green transition-colors text-lg cursor-pointer" aria-label="Close navigation menu">
      ✕
    </button>
  </div>
  <div class="flex flex-col py-4 flex-grow">
    {links.map((link, i) => (
      <a
        href={link.href}
        class="drawer-link text-text-dim hover:text-binary-green hover:bg-system-border-dim px-5 py-3 text-sm font-medium transition-colors duration-200"
      >
        <span class="text-text-deemphasized mr-1" aria-hidden="true">❯</span> {link.name.toLowerCase()}
      </a>
    ))}
  </div>
  <div class="px-5 py-4 border-t border-system-border">
    <a href="/LEGASPI_RESUME.pdf" target="_blank" class="flex items-center gap-2 text-caution-yellow hover:text-white text-xs font-mono transition-colors">
      <span aria-hidden="true">📄</span> LEGASPI_RESUME.pdf
    </a>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const hamburger = document.getElementById('hamburger-btn');
    const drawer = document.getElementById('mobile-drawer');
    const backdrop = document.getElementById('drawer-backdrop');
    const closeBtn = document.getElementById('drawer-close');
    const drawerLinks = document.querySelectorAll('.drawer-link');
    const lines = hamburger?.querySelectorAll('.hamburger-line');

    let isOpen = false;

    function openDrawer() {
      isOpen = true;
      drawer?.classList.remove('hidden', 'drawer-close');
      drawer?.classList.add('flex', 'drawer-open');
      backdrop?.classList.remove('hidden');
      hamburger?.setAttribute('aria-expanded', 'true');
      document.body.style.overflow = 'hidden';
    }

    function closeDrawer() {
      isOpen = false;
      drawer?.classList.remove('drawer-open');
      drawer?.classList.add('drawer-close');
      backdrop?.classList.add('hidden');
      hamburger?.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
      
      setTimeout(() => {
        drawer?.classList.add('hidden');
        drawer?.classList.remove('flex', 'drawer-close');
      }, 200);
    }

    hamburger?.addEventListener('click', () => {
      if (isOpen) closeDrawer();
      else openDrawer();
    });

    closeBtn?.addEventListener('click', closeDrawer);
    backdrop?.addEventListener('click', closeDrawer);

    drawerLinks.forEach(link => {
      link.addEventListener('click', () => {
        closeDrawer();
      });
    });

    // Close on Escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && isOpen) closeDrawer();
    });
  });
</script>
